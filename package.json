{
  "name": "koreografeyedemo",
  "version": "0.1.0",
  "license": "ISC",
  "author": "Patrick Hochstenbach <Patrick.Hochstenbach@UGent.be>",
  "description": "Demonstration Koreografeye project",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "orch:demo": "npx orch --info --keep --in in --out out --err err rules/demo.n3",
    "orch:ldn": "npx orch --info --keep --in in --out out --err err rules/ldn.n3",
    "pol": "npx pol --info --keep --single out/demo.ttl",
    "clean": "rm out/* ; rm err/*"
  },
  "dependencies": {
    "koreografeye": "^0.4.6",
    "nodemailer": "^6.9.1"
  }
}
